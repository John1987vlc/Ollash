{
  "name": "ollash-frontend",
  "version": "1.2.0",
  "description": "Ollash frontend build pipeline (Vite + Vitest). Use feature flag USE_VITE_ASSETS=true to activate bundled assets.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "vitest": "^1.6.0",
    "@vitest/coverage-v8": "^1.6.0"
  }
}
