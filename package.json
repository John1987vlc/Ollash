{
  "name": "ollash-frontend",
  "version": "1.2.0",
  "description": "Ollash frontend build pipeline (Vite). Use feature flag USE_VITE_ASSETS=true to activate bundled assets.",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
